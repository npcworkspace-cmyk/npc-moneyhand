interface:
  display_name: "NPC MoneyHand"
  short_description: "Connect and verify MoneyHand through one fixed flow"
  default_prompt: "Use $npc-moneyhand to install or import it if needed, immediately run its fixed connection and automatic acceptance in the same turn without offering them as optional, then follow the bounded result."
