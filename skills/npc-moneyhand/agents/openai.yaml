interface:
  display_name: "NPC MoneyHand"
  short_description: "Detect and control the MoneyHand Chromium extension"
  default_prompt: "Use $npc-moneyhand to run the first-use preflight when needed, then start one task-owned browser controller and complete the browser task safely."
